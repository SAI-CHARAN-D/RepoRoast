# ==================================================
# RepoRoast Environment Configuration Template
# ==================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# Flask Configuration
SECRET_KEY=your-secret-key-here-change-this-in-production

# Google AI (Gemini)
GOOGLE_API_KEY=your-gemini-api-key-here

# Google Cloud Text-to-Speech
# Option 1: Paste JSON content directly (recommended for cloud deployment)
GOOGLE_CLOUD_TTS_JSON={"type":"service_account","project_id":"your-project","private_key_id":"..."}

# Option 2: Use file path (for local development)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/your/service-account-key.json

# ==================================================
# Deployment Notes:
# ==================================================
# For Google Cloud TTS JSON:
# 1. Get your service account key from Firebase/GCP Console
# 2. Minify the JSON to a single line (no newlines)
# 3. Paste it as the value for GOOGLE_CLOUD_TTS_JSON
#
# For local development:
# - Use GOOGLE_APPLICATION_CREDENTIALS with file path
# - Server will auto-reload when you update .env
